var Bad : bool
array Mark[proc] : bool

init (i) { Bad = False && Mark[i] = False }

unsafe () { Bad = True }

transition start ()
requires { Bad = False }
{
}
triggers finish (_ _)

triggered transition finish (x y)
requires { x = y }
{
  Bad := True;
}
